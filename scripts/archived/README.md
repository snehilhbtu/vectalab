# Archived scripts

These scripts were moved from the top-level `scripts/` directory because they're ad-hoc, dated, or experimental and clutter the main scripts area.

They are preserved here for historical reference and can be restored if needed.

If you are reusing or updating an archived script, please move it back to `scripts/` and update the main repo README to document the workflow.
